# 虚拟环境
.venv/

# 二进制安装包
*.whl

# 测试音频（大二进制文件，不入库）
test/*.wav

# 模型权重（GB 级大文件，不入库；放工程目录 model/ 下）
model/

# 个人笔记，不入库
transcribe_cpp_notes.txt

# Python 缓存
__pycache__/
*.pyc

# WebUI 运行时产生的上传文件
uploads/
